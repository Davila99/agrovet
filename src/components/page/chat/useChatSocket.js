// Wrapper re-export to ensure this module parses as plain JS for Vite import-analysis.
// The real implementation lives in useChatSocket.jsx (same directory).
import useChatSocket from './useChatSocket.jsx';
export default useChatSocket;
